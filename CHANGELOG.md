# Changelog

## 0.1.1 — 2026-09-14
드라이런(실제 크루 제출물 1명 · 랜딩 밖 가상 공방 · 모드 ② 가짜 기록 3종 · state.sh) 결과 반영.
- 안전: 스폰지클럽 저장소 안의 하위 폴더(과제 폴더 속 랜딩)도 `state.sh` 가 git 최상위로 판별해 막음. 코드 없음·스폰지클럽 저장소일 때 상태 파일 폴더를 먼저 묻는 Q0 추가. git 이 아니면 커밋 건너뛰기.
- 규칙 충돌 정리: 약관 금지 플랫폼의 "자동 입력 금지"와 홈택스 입력칸 채우기 구분, 스크래핑 대행(아이디·비밀번호 맡김)과 발급 API 인증서 등록(`[서명]`) 구분, 브라우저 결정 규칙에서 반복 주고받기(ego lite)를 한 번짜리(Claude in Chrome)보다 먼저.
- 점수: 전체 주당 분 대신 **줄이는(분)** 으로 채점, G3 로 다시 정의하면 d·줄이는(분)을 다시 매김, 게이트 G5(4주 이상 계속되는 일) 추가, 돈·법 문제 이력이 있는 r3 일은 「위험 우선」 으로 S6 에 올림. 주기에 반기·2년·건마다 추가. 계산 예·S6 예시를 규칙과 일치시킴.
- 모드 ②: 추천 조건을 log 2줄 이상 + (5줄 또는 7일)로, 평균은 기준선과 같은 주기 단위(`cycle`)로. 깊게/넓게 경계를 사람만 몫 70% 로 명확히 하고 정보성 발송은 자동화 가능 몫으로. status `대체됨` 추가, v3 연결은 서로 다른 일 수로 세고 `connect-plan.md` 로.
- log 템플릿: 「건너뜀」 을 「안 씀(이유)」 과 「(준비물 틀림)」 표시로 나누고 손으로 한 부분에 태그·분.
- 인터뷰: 사용자에게 ID·기호 대신 값·기준일, S1 사업 모양에 납품·공방, 질문 상한에서 모드·폴더 질문 제외, 시간 표기 통일.

## 0.1.0 — 2026-09-13
- 첫 배포. 스펀지클럽 3기 6회차 「내 비즈니스를 도와주는 운영 OS」 과제 02 도우미.
- 모드 3개: ① 처음(인터뷰 → 운영 지도 → v1 선택 → 기획안 v1·v2·v3 → v1 구현 → log 시작) ② 다시(사용 기록 → v1.1 / 깊게 / 넓게 / v3 연결 판정) ③ 지도만.
- v1 규칙 고정: 일 하나 · 반자동(사람이 확인하고 버튼 하나) · 하루 안에. 가벼움 사다리 5칸(예약 작업 → 시트+Apps Script → 브라우저 에이전트 → 어드민 화면 → 새 앱), 기존 저장소 연결이 기본.
- 운영 영역 9개 카탈로그(세금계산서·직원·정산·메시지·예약주문·문의리뷰·발행·숫자·1차 전환 흐름), 점수식 주당(분) × 실수 가중치 ÷ 준비 난이도 + 게이트 G1~G4.
- 사람만 하는 단계 태그 5개와 예외 2개, 약관이 자동화를 막는 곳, 저장하지 않는 개인정보.
- 브라우저 에이전트 4종(Claude in Chrome · ego lite · Playwright · Aside) 결정 규칙.
- 현황 기준일 2026-09-13: 전자세금계산서 API 단가, 알림톡 단가·심사 기간, 친구톡 종료, 홈택스 사업자 간편인증, 브라우저 도구 현황.
- `scripts/state.sh` (상태 파일로 모드 추천, 읽기 전용) · `scripts/check.sh`.
